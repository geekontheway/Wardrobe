%html
  %head
    %script{:src => "http://ajax.googleapis.com/ajax/libs/jquery/1.4.2/jquery.min.js"}
    :javascript
      jQuery(document).ready(function($) {
        ws = new WebSocket("ws://127.0.0.1:8080");
        ws.onopen = function() {
          ws.send("hi man");
        }
        ws.onmessage = function(msg) {
          alert(msg.data);
        }
        ws.send("cmon and work");

      });
  %body
    %div{:id => "debug"}
    %div{:id => "msg"}
